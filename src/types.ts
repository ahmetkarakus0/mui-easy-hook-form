import { EGender, EMaritalStatus } from "./enums";

export type TGender = {
  value: EGender;
  label: string;
};

export type TMaritalStatus = {
  value: EMaritalStatus;
  label: string;
};

export type TSatisfactionMark = {
  value: number;
  label: string;
};
